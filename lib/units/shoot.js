const stdin = process.stdin;
stdin.setRawMode(true);
stdin.resume();
stdin.setEncoding('utf8');

cons keyProcessor = (key) => {
  if (key === "\u0020") {
    arr[x][y] = currCanonPos
    arr[x-1][y] = 'i'
    x--
    console.clear()
    print(arr)
  }
};